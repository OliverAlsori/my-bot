services:
  - type: worker
    name: telegram-accounting-bot
    env: node
    plan: starter
    autoDeploy: true
    buildCommand: npm ci && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: BOT_TOKEN=123456:ABC...
        sync: false



